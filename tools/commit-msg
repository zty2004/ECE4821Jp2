#!/bin/bash

if ! head -n 1 "$1" | grep -qE "^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\([a-zA-Z0-9_-]+\))?: .+$"; then
  echo "Error: non-conventional commit message"
  exit 1
fi
